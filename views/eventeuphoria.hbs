<!DOCTYPE html>
<html class="dark scroll-smooth" lang="en">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>EVENT EUPHORIA '26 | Annual Fest | SIRAJUL IRFAN</title>
    <!-- PWA & Theme Color -->
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="#070a12" />

    <!-- Favicon Links -->
    <link rel="icon" type="image/png" sizes="32x32" href="/images/logo-32.png">
    <link rel="icon" type="image/png" sizes="192x192" href="/images/logo-192.png">
    <link rel="apple-touch-icon" sizes="192x192" href="/images/logo-192.png">
    <link rel="shortcut icon" href="/images/favicon.ico">

    <!-- Primary SEO Meta Tags -->
    <meta name="description"
        content="Official Event Euphoria '26 Arts & Cultural Festival Portal for Sirajul Irfan Da'wa Dars, Perimbalam, Kodampuzha. Explore live house standings, competition schedules, candidate results, and festival gallery." />
    <meta name="keywords"
        content="Event Euphoria, Event Euphoria 2026, Sirajul Irfan Fest, Sirajul Irfan Annual Fest, Islamic Arts Fest, Kodampuzha Arts Fest, Sirajul Irfan House Standings, Kodampuzha Competition" />
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
    <link rel="canonical" href="https://sirajulirfan.com/eventeuphoria" />

    <!-- Open Graph / Facebook SEO -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="SIRAJUL IRFAN" />
    <meta property="og:title" content="EVENT EUPHORIA '26 | Annual Arts & Cultural Fest | SIRAJUL IRFAN" />
    <meta property="og:description"
        content="Official Event Euphoria '26 Festival Portal. Track live house scoreboards, event results, schedule, and highlights from Sirajul Irfan Da'wa Dars." />
    <meta property="og:url" content="https://sirajulirfan.com/eventeuphoria" />
    <meta property="og:image" content="https://sirajulirfan.com/images/logo.png" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter Card SEO -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="EVENT EUPHORIA '26 | Annual Arts & Cultural Fest | SIRAJUL IRFAN" />
    <meta name="twitter:description"
        content="Official Event Euphoria '26 Festival Portal. Track live house scoreboards, event results, schedule, and highlights from Sirajul Irfan Da'wa Dars." />
    <meta name="twitter:image" content="https://sirajulirfan.com/images/logo.png" />

    <!-- Structured Data / JSON-LD Schema Markup for Search Engines -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": ["Event", "Festival", "EducationalEvent"],
          "@id": "https://sirajulirfan.com/eventeuphoria#event",
          "name": "Event Euphoria '26 - Annual Arts & Cultural Fest",
          "alternateName": ["Euphoria 2026", "Sirajul Irfan Annual Festival", "യുഫോറിയ '26"],
          "description": "The flagship annual arts, cultural, and academic competition festival of Sirajul Irfan Da'wa Dars, featuring multi-house competitions, live scoreboards, and public events.",
          "url": "https://sirajulirfan.com/eventeuphoria",
          "startDate": "2026-10-10T06:00:00+05:30",
          "endDate": "2026-10-16T22:00:00+05:30",
          "eventAttendanceMode": "https://schema.org/OfflineEventAttendanceMode",
          "eventStatus": "https://schema.org/EventScheduled",
          "image": [
            "https://sirajulirfan.com/images/logo.png"
          ],
          "location": {
            "@type": "Place",
            "name": "Sirajul Irfan Da'wa Dars Campus",
            "address": {
              "@type": "PostalAddress",
              "streetAddress": "Perimbalam Markaz, Kodampuzha Da'wa College Off Campus",
              "addressLocality": "Kodampuzha, Manjeri",
              "addressRegion": "Kerala",
              "postalCode": "676121",
              "addressCountry": "IN"
            },
            "geo": {
              "@type": "GeoCoordinates",
              "latitude": "11.1271",
              "longitude": "76.1242"
            }
          },
          "organizer": {
            "@type": "EducationalOrganization",
            "@id": "https://sirajulirfan.com/#organization",
            "name": "Sirajul Irfan Da'wa Dars",
            "url": "https://sirajulirfan.com",
            "logo": "https://sirajulirfan.com/images/logo.png"
          }
        },
        {
          "@type": "BreadcrumbList",
          "@id": "https://sirajulirfan.com/eventeuphoria#breadcrumb",
          "itemListElement": [
            {
              "@type": "ListItem",
              "position": 1,
              "name": "Home",
              "item": "https://sirajulirfan.com"
            },
            {
              "@type": "ListItem",
              "position": 2,
              "name": "Event Euphoria '26",
              "item": "https://sirajulirfan.com/eventeuphoria"
            }
          ]
        }
      ]
    }
    </script>

    <!-- Google Fonts & Material Symbols -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@500;600;700&family=Inter:wght@300;400;500;600;700;800&family=Outfit:wght@400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
        rel="stylesheet" />

    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: {
                            gold: '#f59e0b',
                            goldLight: '#fbbf24',
                            purple: '#8b5cf6',
                            deepDark: '#070a12',
                            cardDark: '#0f172a',
                            cyan: '#06b6d4'
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'Outfit', 'sans-serif'],
                        heading: ['Outfit', 'Chakra Petch', 'sans-serif']
                    }
                }
            }
        }
    </script>

    <!-- Custom Sahityotsav Aesthetics Styling & Custom Font Definition -->
    <style>
        @font-face {
            font-family: 'TraditionalCivilization';
            src: url('/Font/Traditional%20Civilization%20Demo.ttf') format('truetype'),
                url('/Font/Traditional Civilization Demo.ttf') format('truetype');
            font-weight: normal;
            font-style: normal;
            font-display: swap;
        }

        .font-custom-heading {
            font-family: 'TraditionalCivilization', 'Outfit', 'Inter', sans-serif;
        }

        /* Hero Heading Euphoria Custom Font & Organic Floating Letter Animation */
        @keyframes floatBounceA {

            0%,
            100% {
                transform: translateY(0px) rotate(0deg);
            }

            50% {
                transform: translateY(-14px) rotate(-1.5deg);
            }
        }

        @keyframes floatBounceB {

            0%,
            100% {
                transform: translateY(0px) rotate(0deg);
            }

            45% {
                transform: translateY(-18px) rotate(2deg);
            }
        }

        @keyframes floatBounceC {

            0%,
            100% {
                transform: translateY(0px) rotate(0deg);
            }

            55% {
                transform: translateY(-10px) rotate(-1deg);
            }
        }

        .hero-heading-euphoria {
            font-family: 'TraditionalCivilization', 'Outfit', 'Inter', sans-serif;
            color: #ffffff;
            display: block;
            width: 100%;
            max-width: 100%;
            margin-left: auto;
            margin-right: auto;
            text-align: center;
            font-size: clamp(3rem, 14vw, 6.5rem);
            line-height: 1.1;
            box-sizing: border-box;
            text-shadow: none;
            filter: none;
        }

        .hero-heading-euphoria .letter {
            display: inline-block;
            color: #ffffff;
            -webkit-text-fill-color: #ffffff;
            text-shadow: none;
            transition: color 0.3s ease-in-out, -webkit-text-fill-color 0.3s ease-in-out;
            cursor: pointer;
            will-change: transform;
        }

        /* Randomized Organic Floating & Bouncing Delays, Durations & Animations */
        .hero-heading-euphoria .letter-1 {
            animation: floatBounceA 3.2s ease-in-out infinite 0s;
        }

        .hero-heading-euphoria .letter-2 {
            animation: floatBounceB 2.8s ease-in-out infinite 0.3s;
        }

        .hero-heading-euphoria .letter-3 {
            animation: floatBounceC 3.5s ease-in-out infinite 0.7s;
        }

        .hero-heading-euphoria .letter-4 {
            animation: floatBounceA 3.0s ease-in-out infinite 0.2s;
        }

        .hero-heading-euphoria .letter-5 {
            animation: floatBounceB 3.4s ease-in-out infinite 0.5s;
        }

        .hero-heading-euphoria .letter-6 {
            animation: floatBounceC 2.7s ease-in-out infinite 0.1s;
        }

        .hero-heading-euphoria .letter-7 {
            animation: floatBounceA 3.3s ease-in-out infinite 0.6s;
        }

        .hero-heading-euphoria .letter-8 {
            animation: floatBounceB 2.9s ease-in-out infinite 0.4s;
        }

        /* Targeted Hover: Only the specific hovered letter changes color */
        .hero-heading-euphoria .letter-1:hover {
            color: #06b6d4;
            -webkit-text-fill-color: #06b6d4;
        }

        .hero-heading-euphoria .letter-2:hover {
            color: #8b5cf6;
            -webkit-text-fill-color: #8b5cf6;
        }

        .hero-heading-euphoria .letter-3:hover {
            color: #ec4899;
            -webkit-text-fill-color: #ec4899;
        }

        .hero-heading-euphoria .letter-4:hover {
            color: #f43f5e;
            -webkit-text-fill-color: #f43f5e;
        }

        .hero-heading-euphoria .letter-5:hover {
            color: #f59e0b;
            -webkit-text-fill-color: #f59e0b;
        }

        .hero-heading-euphoria .letter-6:hover {
            color: #10b981;
            -webkit-text-fill-color: #10b981;
        }

        .hero-heading-euphoria .letter-7:hover {
            color: #38bdf8;
            -webkit-text-fill-color: #38bdf8;
        }

        .hero-heading-euphoria .letter-8:hover {
            color: #a855f7;
            -webkit-text-fill-color: #a855f7;
        }

        .material-symbols-outlined {
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
        }

        .glass-panel {
            background: rgba(15, 23, 42, 0.7);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.08);
        }

        .glass-card {
            background: rgba(11, 17, 32, 0.85);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.07);
        }

        .glass-nav {
            background: rgba(7, 10, 18, 0.9);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
        }

        .gradient-text-gold {
            background: linear-gradient(135deg, #fef08a 0%, #f59e0b 50%, #d97706 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .gradient-text-vibrant {
            background: linear-gradient(135deg, #ffffff 0%, #cbd5e1 50%, #94a3b8 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .bg-grid-mesh {
            background-image: radial-gradient(rgba(245, 158, 11, 0.12) 1px, transparent 1px);
            background-size: 32px 32px;
        }

        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #070a12;
        }

        ::-webkit-scrollbar-thumb {
            background: #334155;
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #f59e0b;
        }
    </style>
</head>

<body
    class="bg-[#070a12] text-slate-100 font-sans antialiased selection:bg-amber-500 selection:text-slate-950 overflow-x-hidden">

    <!-- Navbar: Logo (Left), Live Countdown (Middle), Results Button (Right) -->
    <header class="fixed top-0 left-0 right-0 z-50 glass-nav border-b border-slate-800/80 transition-all duration-300">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 sm:h-20 flex items-center justify-between gap-2 sm:gap-4">
            <!-- Left: Brand Logo Only -->
            <a href="/eventeuphoria"
                class="flex items-center group transition-transform duration-300 hover:scale-105 shrink-0">
                <img src="/images/EUPHORIA LOGO.svg" alt="Event Euphoria '26 Logo"
                    class="h-7 sm:h-9 md:h-10 w-auto object-contain brightness-0 invert drop-shadow-md" />
            </a>

            <!-- Middle: Live Time Countdown Timer Widget -->
            <div
                class="flex items-center gap-1.5 sm:gap-2 px-3 sm:px-4 py-1.5 rounded-full bg-slate-900/80 border border-slate-800/90 shadow-inner text-xs">
                <span class="flex h-2 w-2 relative shrink-0">
                    <span
                        class="animate-ping absolute inline-flex h-full w-full rounded-full bg-amber-400 opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-amber-500"></span>
                </span>
                <span
                    class="hidden md:inline text-[10px] font-bold text-amber-400/90 uppercase tracking-wider mr-0.5">Countdown:</span>
                <div
                    class="flex items-center gap-1 font-mono font-bold text-xs sm:text-sm tracking-tight text-slate-100">
                    <div class="flex items-baseline gap-0.5">
                        <span id="nav-cd-days" class="text-amber-400 font-extrabold">00</span>
                        <span class="text-[9px] text-slate-400 font-sans font-semibold">d</span>
                    </div>
                    <span class="text-slate-600">:</span>
                    <div class="flex items-baseline gap-0.5">
                        <span id="nav-cd-hours" class="text-white font-extrabold">00</span>
                        <span class="text-[9px] text-slate-400 font-sans font-semibold">h</span>
                    </div>
                    <span class="text-slate-600">:</span>
                    <div class="flex items-baseline gap-0.5">
                        <span id="nav-cd-mins" class="text-white font-extrabold">00</span>
                        <span class="text-[9px] text-slate-400 font-sans font-semibold">m</span>
                    </div>
                    <span class="text-slate-600">:</span>
                    <div class="flex items-baseline gap-0.5">
                        <span id="nav-cd-secs" class="text-cyan-400 font-extrabold">00</span>
                        <span class="text-[9px] text-slate-400 font-sans font-semibold">s</span>
                    </div>
                </div>
            </div>

            <!-- Right: Results Button Only (Hidden on Mobile) -->
            <a href="/eventeuphoria/result"
                class="hidden sm:flex items-center gap-1.5 px-3.5 sm:px-4 py-2 rounded-xl bg-gradient-to-r from-amber-500 to-amber-600 text-slate-950 font-bold text-xs shadow-lg shadow-amber-500/20 hover:brightness-110 active:scale-95 transition-all shrink-0">
                <span class="material-symbols-outlined text-sm sm:text-base">trophy</span>
                <span>Results</span>
            </a>
        </div>
    </header>

    <!-- ========================================== -->
    <!-- 1. HERO SECTION (Clean SVG Logo Heading)   -->
    <!-- ========================================== -->
    <section class="relative min-h-[85vh] sm:min-h-[88vh] flex flex-col items-center justify-center pt-24 pb-12 sm:pt-36 sm:pb-24 overflow-hidden bg-grid-mesh">
        <!-- Background Ambient Glow -->
        <div
            class="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[350px] bg-amber-500/10 blur-[130px] rounded-full pointer-events-none">
        </div>
        <div
            class="absolute top-1/3 right-10 w-[400px] h-[300px] bg-purple-500/10 blur-[140px] rounded-full pointer-events-none">
        </div>

        <div
            class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-20 text-center flex flex-col items-center justify-center w-full">

            <!-- Edition Pill Badge -->
            <div
                class="relative z-30 inline-flex items-center gap-1.5 sm:gap-2 px-3 py-1 sm:px-4 sm:py-1.5 rounded-full bg-slate-900/95 border border-amber-500/40 text-amber-300 font-bold text-[9.5px] xs:text-[10px] sm:text-xs uppercase tracking-wider sm:tracking-widest mb-3 sm:mb-5 shadow-lg shadow-amber-500/10">
                <span class="w-1.5 h-1.5 sm:w-2 sm:h-2 rounded-full bg-amber-400 animate-ping shrink-0"></span>
                <span class="whitespace-nowrap">7TH ANNUAL ARTS & CULTURAL FESTIVAL</span>
            </div>

            <!-- Custom Font Hero Text Heading -->
            <h1
                class="hero-heading-euphoria font-custom-heading text-5xl sm:text-7xl lg:text-8xl w-full max-w-5xl mx-auto block text-center font-normal tracking-wide leading-none mb-4 sm:mb-6 select-none cursor-pointer drop-shadow-2xl">
                <span class="letter letter-1">E</span><span class="letter letter-2">u</span><span
                    class="letter letter-3">p</span><span class="letter letter-4">h</span><span
                    class="letter letter-5">o</span><span class="letter letter-6">r</span><span
                    class="letter letter-7">i</span><span class="letter letter-8">a</span>
            </h1>

            <!-- Institution & Subtitle (Hidden on Mobile) -->
            <p
                class="hidden sm:block max-w-xl mx-auto text-sm md:text-base px-4 text-slate-300/80 font-normal leading-relaxed mb-6 sm:mb-8 text-center">
                Sirajul Irfan Da'wa Dars Flagship Cultural & Academic Competition. Celebrating talent, literary
                perfection, and academic excellence.
            </p>

            <!-- Date & Location Badges (Side-by-side inline on the exact same line on mobile) -->
            <div
                class="flex flex-row flex-nowrap items-center justify-center gap-1.5 sm:gap-4 mb-5 sm:mb-8 text-[10px] sm:text-sm font-semibold text-slate-300 w-full max-w-full px-1">
                <div
                    class="flex items-center gap-1 sm:gap-2 px-2.5 sm:px-4 py-1.5 sm:py-2 rounded-lg sm:rounded-xl bg-slate-900/80 border border-slate-800 text-amber-300 shrink-0 whitespace-nowrap">
                    <span class="material-symbols-outlined text-amber-400 text-sm sm:text-lg shrink-0">calendar_month</span>
                    <span>October 10 – 11</span>
                </div>
                <div
                    class="flex items-center gap-1 sm:gap-2 px-2.5 sm:px-4 py-1.5 sm:py-2 rounded-lg sm:rounded-xl bg-slate-900/80 border border-slate-800 text-slate-200 shrink-0 whitespace-nowrap">
                    <span class="material-symbols-outlined text-cyan-400 text-sm sm:text-lg shrink-0">location_on</span>
                    <span>Sirajul Irfan Off Campus</span>
                </div>
            </div>

            <!-- Prominent Main Actions (VIEW RESULTS) Directly underneath Date & Location -->
            <div class="flex items-center justify-center w-full max-w-xs mx-auto">
                <!-- Primary Button: Solid Vibrant Red/Orange-Red Pill with Right Arrow -->
                <a href="/eventeuphoria/result"
                    class="w-full sm:w-auto min-w-[190px] flex items-center justify-center gap-2 px-6 sm:px-8 py-3 sm:py-3.5 rounded-full bg-gradient-to-r from-red-600 via-rose-600 to-orange-600 hover:from-red-500 hover:to-orange-500 text-white font-heading font-extrabold text-xs sm:text-sm uppercase tracking-wider shadow-lg shadow-red-600/30 hover:shadow-red-600/50 hover:scale-[1.03] active:scale-95 transition-all">
                    <span>VIEW RESULTS</span>
                    <span class="material-symbols-outlined text-base sm:text-lg">arrow_forward</span>
                </a>
            </div>

        </div>
    </section>

    <!-- ========================================== -->
    <!-- 2. COUNTDOWN TIMER SECTION                 -->
    <!-- ========================================== -->
    <section id="countdown" class="py-12 bg-slate-950/60 border-y border-slate-800/80">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex flex-col md:flex-row items-center justify-between gap-8">
                <div>
                    <span class="text-xs uppercase tracking-widest text-amber-400 font-bold">FESTIVAL COUNTDOWN</span>
                    <h2 class="text-2xl font-heading font-bold text-white mt-1">Grand Finale Countdown</h2>
                </div>
                <div class="grid grid-cols-4 gap-3 sm:gap-6 text-center">
                    <div class="glass-card px-4 py-3 rounded-2xl min-w-[70px]">
                        <span id="cd-days"
                            class="block text-2xl sm:text-3xl font-heading font-bold text-amber-400">05</span>
                        <span class="text-[10px] text-slate-400 uppercase tracking-wider">Days</span>
                    </div>
                    <div class="glass-card px-4 py-3 rounded-2xl min-w-[70px]">
                        <span id="cd-hours"
                            class="block text-2xl sm:text-3xl font-heading font-bold text-white">14</span>
                        <span class="text-[10px] text-slate-400 uppercase tracking-wider">Hours</span>
                    </div>
                    <div class="glass-card px-4 py-3 rounded-2xl min-w-[70px]">
                        <span id="cd-mins"
                            class="block text-2xl sm:text-3xl font-heading font-bold text-white">32</span>
                        <span class="text-[10px] text-slate-400 uppercase tracking-wider">Mins</span>
                    </div>
                    <div class="glass-card px-4 py-3 rounded-2xl min-w-[70px]">
                        <span id="cd-secs"
                            class="block text-2xl sm:text-3xl font-heading font-bold text-cyan-400">45</span>
                        <span class="text-[10px] text-slate-400 uppercase tracking-wider">Secs</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ========================================== -->
    <!-- 3. LIVE COMPETITION RESULTS & SCOREBOARD   -->
    <!-- ========================================== -->
    <section id="houses" class="py-20 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        <!-- Section Header -->
        <div class="flex flex-col md:flex-row md:items-end justify-between mb-12 gap-4">
            <div>
                <div
                    class="inline-flex items-center gap-2 px-3.5 py-1 rounded-full bg-amber-500/10 border border-amber-500/30 text-amber-400 font-semibold text-xs uppercase tracking-widest mb-3">
                    <span class="w-2 h-2 rounded-full bg-amber-400 animate-ping"></span>
                    <span>LIVE RESULTS</span>
                </div>
                <h2 class="text-3xl sm:text-4xl md:text-5xl font-heading font-extrabold text-white tracking-tight">
                    Published Competition Results</h2>
                <p class="text-slate-400 text-sm mt-2 max-w-2xl">Real-time candidate results, house leaderboards, and
                    instant competition result lookup.</p>
            </div>
            <a href="/eventeuphoria/result"
                class="inline-flex items-center gap-2 px-5 py-2.5 rounded-xl bg-slate-900 border border-amber-500/30 hover:border-amber-500 text-amber-400 text-xs font-bold transition-all shadow-lg hover:brightness-110 shrink-0">
                <span class="material-symbols-outlined text-base">apps</span> View Full Results Portal
            </a>
        </div>

        <!-- 2-Column Sahithyolsav Layout: Left = Standings, Right = Find a Result -->
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch mb-16">

            <!-- LEFT COLUMN: WHO'S LEADING / TOP STANDINGS (lg:col-span-7) -->
            <div
                class="lg:col-span-7 glass-panel p-6 sm:p-8 rounded-3xl border border-slate-800 shadow-2xl flex flex-col justify-between">
                <div>
                    <!-- Header -->
                    <div class="flex items-center justify-between border-b border-slate-800/80 pb-4 mb-6">
                        <div class="flex items-center gap-3">
                            <div
                                class="w-10 h-10 rounded-xl bg-amber-500/10 border border-amber-500/30 flex items-center justify-center text-amber-400">
                                <span class="material-symbols-outlined text-xl">trophy</span>
                            </div>
                            <div>
                                <h3 class="font-heading font-extrabold text-xl text-white tracking-tight">WHO'S LEADING?
                                </h3>
                                <p class="text-xs text-slate-400">Current House Standings & Total Points</p>
                            </div>
                        </div>
                        <span
                            class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/20 text-[10px] font-bold uppercase tracking-wider">
                            <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span> LIVE
                        </span>
                    </div>

                    <!-- Standings Leaderboard Items -->
                    {{#if houses.length}}
                    <div class="space-y-4 mb-6">
                        {{#each houses}}
                        <div
                            class="p-4 rounded-2xl bg-slate-900/80 border border-slate-800/80 hover:border-amber-500/40 transition-all flex items-center justify-between gap-4 group">
                            <div class="flex items-center gap-3.5 min-w-0">
                                <span
                                    class="w-9 h-9 rounded-xl flex items-center justify-center font-heading font-extrabold text-sm shrink-0 {{#if (eq this.rank 1)}}bg-gradient-to-br from-amber-400 to-yellow-500 text-slate-950 shadow-lg shadow-amber-500/20{{else if (eq this.rank 2)}}bg-slate-300 text-slate-950{{else if (eq this.rank 3)}}bg-amber-700 text-white{{else}}bg-slate-800 text-slate-400{{/if}}">
                                    {{this.rank}}
                                </span>
                                <div class="min-w-0">
                                    <div class="flex items-center gap-2">
                                        <h4
                                            class="font-heading font-bold text-white text-base group-hover:text-amber-300 transition-colors truncate">
                                            {{this.name}}</h4>
                                        {{#if this.badge}}
                                        <span
                                            class="text-[9px] font-bold px-2 py-0.5 rounded-full {{this.badgeClass}} shrink-0 uppercase tracking-wider">{{this.badge}}</span>
                                        {{/if}}
                                    </div>
                                    <!-- Animated Progress Line -->
                                    <div class="w-36 sm:w-48 bg-slate-800 h-2 rounded-full mt-2 overflow-hidden">
                                        <div class="h-full bg-gradient-to-r {{this.barGradient}} rounded-full transition-all duration-1000"
                                            style="width: {{this.progress}}%"></div>
                                    </div>
                                </div>
                            </div>
                            <div class="text-right shrink-0">
                                <span
                                    class="font-heading font-extrabold text-lg sm:text-xl text-amber-400 block">{{this.points}}</span>
                                <span
                                    class="text-[10px] text-slate-400 uppercase tracking-wider font-semibold">PTS</span>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                    {{else}}
                    <div
                        class="p-8 text-center rounded-2xl bg-slate-900/50 border border-slate-800/50 text-slate-400 text-xs italic mb-6">
                        Standings compiling...
                    </div>
                    {{/if}}
                </div>

                <!-- Footer Action Link -->
                <a href="/eventeuphoria/result"
                    class="inline-flex items-center justify-center gap-2 w-full py-3.5 px-4 rounded-2xl bg-slate-900/90 hover:bg-slate-800 text-amber-400 hover:text-amber-300 border border-amber-500/20 hover:border-amber-500/40 text-xs font-bold uppercase tracking-wider transition-all shadow-md active:scale-95">
                    <span>SEE ALL HOUSES & FULL STANDINGS</span>
                    <span class="material-symbols-outlined text-base">east</span>
                </a>
            </div>

            <!-- RIGHT COLUMN: FIND A RESULT BOX (lg:col-span-5) -->
            <div
                class="lg:col-span-5 glass-panel p-6 sm:p-8 rounded-3xl border border-amber-500/20 shadow-2xl flex flex-col justify-between bg-gradient-to-b from-slate-900/95 via-slate-900/85 to-slate-950 relative overflow-hidden">
                <div
                    class="absolute -top-16 -right-16 w-48 h-48 bg-amber-500/10 blur-3xl rounded-full pointer-events-none">
                </div>

                <div>
                    <!-- Card Header -->
                    <div class="border-b border-slate-800/80 pb-4 mb-6">
                        <h3 class="text-xl font-heading font-extrabold text-white flex items-center gap-2.5">
                            <span class="material-symbols-outlined text-amber-400 text-2xl">search</span>
                            <span>FIND A RESULT</span>
                        </h3>
                        <p class="text-slate-300 text-xs mt-2 leading-relaxed">
                            Pick a category and competition to jump straight to its result.
                        </p>
                    </div>

                    <!-- Category Dropdown -->
                    <div class="mb-5">
                        <label for="find-result-category"
                            class="block text-xs font-bold uppercase tracking-wider text-slate-400 mb-2">Category</label>
                        <div class="relative">
                            <select id="find-result-category"
                                class="w-full px-4 py-3.5 rounded-xl bg-slate-950 border border-slate-800 text-slate-100 font-semibold text-sm focus:border-amber-400 focus:outline-none transition-all cursor-pointer appearance-none pr-10">
                                <option value="">All categories</option>
                                <option value="Alpha Zone">Alpha Zone</option>
                                <option value="Core Zone">Core Zone</option>
                                <option value="Prime Zone">Prime Zone</option>
                                <option value="General">General</option>
                            </select>
                            <span
                                class="material-symbols-outlined absolute right-3 top-1/2 -translate-y-1/2 text-slate-500 pointer-events-none">expand_more</span>
                        </div>
                    </div>

                    <!-- Competition Dropdown -->
                    <div class="mb-6">
                        <label for="find-result-competition"
                            class="block text-xs font-bold uppercase tracking-wider text-slate-400 mb-2">Competition</label>
                        <div class="relative">
                            <select id="find-result-competition"
                                class="w-full px-4 py-3.5 rounded-xl bg-slate-950 border border-slate-800 text-slate-100 font-semibold text-sm focus:border-amber-400 focus:outline-none transition-all cursor-pointer appearance-none pr-10">
                                <option value="">All competitions</option>
                            </select>
                            <span
                                class="material-symbols-outlined absolute right-3 top-1/2 -translate-y-1/2 text-slate-500 pointer-events-none">expand_more</span>
                        </div>
                    </div>
                </div>

                <!-- Action Button -->
                <button id="btn-get-result" onclick="navigateToSelectedResult()"
                    class="w-full py-4 px-6 rounded-2xl bg-gradient-to-r from-amber-500 via-amber-400 to-yellow-500 hover:brightness-110 active:scale-95 text-slate-950 font-heading font-extrabold text-sm uppercase tracking-wider shadow-lg shadow-amber-500/25 flex items-center justify-center gap-2 transition-all cursor-pointer">
                    <span>GET RESULT</span>
                    <span class="material-symbols-outlined text-xl">arrow_forward</span>
                </button>
            </div>

        </div>

    </section>

    <!-- FestFlow Data Script & Dynamic Search Controller -->
    <script id="festflow-competitions-data" type="application/json">
    {{{json competitions}}}
    </script>
    <script>
            (function () {
                let comps = [];
                try {
                    const rawJson = document.getElementById('festflow-competitions-data')?.textContent;
                    comps = rawJson ? JSON.parse(rawJson) : [];
                } catch (e) {
                    console.error('Error parsing festflow competitions JSON:', e);
                }

                const catSelect = document.getElementById('find-result-category');
                const compSelect = document.getElementById('find-result-competition');

                if (!catSelect || !compSelect) return;

                const PREDEFINED_CATEGORIES = [
                    'Alpha Zone',
                    'Core Zone',
                    'Prime Zone',
                    'General'
                ];

                // Strictly populate the exact predefined categories list
                catSelect.innerHTML = '<option value="">All categories</option>';
                PREDEFINED_CATEGORIES.forEach(cat => {
                    const opt = document.createElement('option');
                    opt.value = cat;
                    opt.textContent = cat;
                    catSelect.appendChild(opt);
                });

                function matchesCategory(compCategory, targetCategory) {
                    if (!compCategory || !targetCategory) return false;
                    const c = String(compCategory).toLowerCase().trim();
                    const t = String(targetCategory).toLowerCase().trim();

                    if (c === t) return true;
                    if (t.includes('alpha') && c.includes('alpha')) return true;
                    if (t.includes('core') && c.includes('core')) return true;
                    if (t.includes('prime') && c.includes('prime')) return true;
                    if (t.includes('general') && c.includes('general')) return true;
                    return c.includes(t) || t.includes(c);
                }

                function populateCompetitions(filteredComps) {
                    compSelect.innerHTML = '<option value="">All competitions</option>';
                    if (!filteredComps || filteredComps.length === 0) {
                        const opt = document.createElement('option');
                        opt.value = '';
                        opt.textContent = 'No competitions in this category';
                        opt.disabled = true;
                        compSelect.appendChild(opt);
                        return;
                    }
                    filteredComps.forEach(comp => {
                        const opt = document.createElement('option');
                        opt.value = comp.id || '';
                        const codeStr = comp.formattedCode ? `#${comp.formattedCode} - ` : '';
                        opt.textContent = `${codeStr}${comp.title}`;
                        compSelect.appendChild(opt);
                    });
                }

                // Initial populate
                populateCompetitions(comps);

                // On category change
                catSelect.addEventListener('change', function () {
                    const selectedCat = this.value;
                    if (!selectedCat) {
                        populateCompetitions(comps);
                    } else {
                        const filtered = comps.filter(c => matchesCategory(c.category, selectedCat));
                        populateCompetitions(filtered);
                    }
                });
            })();

        function navigateToSelectedResult() {
            const compId = document.getElementById('find-result-competition')?.value;
            const cat = document.getElementById('find-result-category')?.value;

            if (compId) {
                window.location.href = `/eventeuphoria/result?id=${encodeURIComponent(compId)}`;
            } else if (cat) {
                window.location.href = `/eventeuphoria/result?category=${encodeURIComponent(cat)}`;
            } else {
                window.location.href = `/eventeuphoria/result`;
            }
        }
    </script>

    <!-- Footer -->
    <footer class="py-12 bg-[#05070d] border-t border-slate-900 text-center text-xs text-slate-500">
        <div class="max-w-7xl mx-auto px-4">
            <p>© 2026 Sirajul Irfan Da'wa Dars. Official Event Euphoria Festival Engine.</p>
            <div class="mt-4 flex justify-center gap-6 text-slate-400">
                <a href="/" class="hover:text-white">Main Website</a>
                <a href="/eventeuphoria/result" class="hover:text-amber-400">Official Results</a>
                <a href="/#contact" class="hover:text-white">Contact Administration</a>
            </div>
        </div>
    </footer>

    <!-- Countdown Script -->
    <script>
        // Countdown Timer Script (Target: October 10, 2026 at 06:00 AM IST)
        const targetDate = new Date('2026-10-10T06:00:00+05:30').getTime();
        function updateCD() {
            const now = new Date().getTime();
            const diff = targetDate - now;
            if (diff > 0) {
                const days = String(Math.floor(diff / (1000 * 60 * 60 * 24))).padStart(2, '0');
                const hours = String(Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60))).padStart(2, '0');
                const mins = String(Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60))).padStart(2, '0');
                const secs = String(Math.floor((diff % (1000 * 60)) / 1000)).padStart(2, '0');

                // Main section countdown elements
                const elDays = document.getElementById('cd-days');
                if (elDays) elDays.innerText = days;
                const elHours = document.getElementById('cd-hours');
                if (elHours) elHours.innerText = hours;
                const elMins = document.getElementById('cd-mins');
                if (elMins) elMins.innerText = mins;
                const elSecs = document.getElementById('cd-secs');
                if (elSecs) elSecs.innerText = secs;

                // Header countdown widget elements
                const navDays = document.getElementById('nav-cd-days');
                if (navDays) navDays.innerText = days;
                const navHours = document.getElementById('nav-cd-hours');
                if (navHours) navHours.innerText = hours;
                const navMins = document.getElementById('nav-cd-mins');
                if (navMins) navMins.innerText = mins;
                const navSecs = document.getElementById('nav-cd-secs');
                if (navSecs) navSecs.innerText = secs;
            }
        }
        setInterval(updateCD, 1000);
        updateCD();

        function shareCompetitionResult(id, title) {
            const shareUrl = window.location.origin + '/eventeuphoria/result#content-' + id;
            const shareTitle = title ? title + ' - Event Euphoria Results' : 'Event Euphoria Competition Results';

            if (navigator.share) {
                navigator.share({
                    title: shareTitle,
                    text: `Official Results for ${title || 'Event Euphoria'}:`,
                    url: shareUrl
                }).catch(() => { });
            } else if (navigator.clipboard) {
                navigator.clipboard.writeText(shareUrl).then(() => {
                    alert('Result link copied to clipboard!');
                }).catch(() => {
                    prompt('Copy result link:', shareUrl);
                });
            } else {
                prompt('Copy result link:', shareUrl);
            }
        }
    </script>
</body>

</html>